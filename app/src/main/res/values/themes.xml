<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TanyAI" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/primary</item>
        <item name="android:statusBarColor">@color/primary</item>
    </style>

    <style name="Theme.TanyAI" parent="Base.Theme.TanyAI" />

    <style name="statusbarWhite" parent="Base.Theme.TanyAI">

        <item name="android:statusBarColor">@color/white</item>
    </style>
    <style name="SplashScreen" parent="Base.Theme.TanyAI">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:statusBarColor">@color/white</item>
    </style>
</resources>